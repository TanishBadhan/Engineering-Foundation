"""10-question practice set for File I/O."""

# Q1 — Paths
# Create a Path for a file named data.txt and print its name, suffix, and parent.

# Q2 — Writing and appending
# Write two lines to a file with w mode, then append a third line with a mode.

# Q3 — Reading methods
# Demonstrate read(), readline(), and readlines() on the same file and explain
# the memory implications of each.

# Q4 — Iteration
# Process a text file line by line and count how many non-empty lines it has.

# Q5 — with / context manager
# Rewrite manual open/close code using with. Explain why this is safer.

# Q6 — Binary files
# Write a few bytes to a .bin file and read them back in binary mode.

# Q7 — CSV
# Create a CSV containing sensor names and readings, then read it with csv.reader
# or csv.DictReader.

# Q8 — JSON
# Save a Python dictionary containing device configuration as JSON and load it
# back. Identify which Python types map naturally to JSON.

# Q9 — File errors
# Handle FileNotFoundError and PermissionError appropriately when opening a file.

# Q10 — Resource design
# Explain why loading a multi-gigabyte text file with read() is risky and write
# a line-by-line approach that keeps memory use small.
