"""10-question practice set for Iterators, Generators & Context Managers."""

# Q1 — Iterator protocol
# Implement an iterator that yields even numbers from 0 through a chosen limit.

# Q2 — __iter__ / __next__
# Explain what each method does and why an iterator normally returns itself from
# __iter__.

# Q3 — StopIteration
# Manually call next() until your iterator is exhausted and handle the final
# StopIteration.

# Q4 — Custom iterator design
# Build an iterator that walks through a list in reverse order.

# Q5 — Generator function
# Write a generator that yields squares from 1 through n.

# Q6 — yield and state
# Call next() repeatedly on your generator and explain which local variables are
# preserved between calls.

# Q7 — Generator expression
# Create a generator for numbers divisible by 3 from 1 to 1,000,000 without
# creating a million-element list.

# Q8 — Lazy evaluation
# Explain why the generator above does not calculate every result immediately.

# Q9 — Context manager
# Implement a small context manager class with __enter__ and __exit__ that logs
# when a resource is acquired and released.

# Q10 — Resource lifecycle
# Explain why with is safer than manual setup/cleanup when an exception can occur
# inside the resource-using block.
